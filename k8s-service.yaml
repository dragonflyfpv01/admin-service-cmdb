apiVersion: v1
kind: Service
metadata:
  name: admin-cmdb-backend-service
  namespace: default
  labels:
    app: admin-cmdb-backend
spec:
  selector:
    app: admin-cmdb-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP